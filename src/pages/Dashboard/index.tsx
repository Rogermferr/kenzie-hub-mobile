import React from "react";
import { ContainerStyle, TextStyle } from "../../styles/global";

const Dashboard = () => {
  return (
    <ContainerStyle>
      <TextStyle>Dashboard</TextStyle>
    </ContainerStyle>
  );
};

export default Dashboard;
